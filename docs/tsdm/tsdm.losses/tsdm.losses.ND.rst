tsdm.losses.ND
==============

.. currentmodule:: tsdm.losses

.. autoclass:: ND
    :show-inheritance:
    :members:

    
    
    .. rubric:: Attributes
    .. autosummary::
    
      ~ND.T_destination
      ~ND.dump_patches
    
    

    
    
    .. rubric:: Methods
    .. autosummary::
    
      ~ND.__init__
      ~ND.add_module
      ~ND.apply
      ~ND.bfloat16
      ~ND.buffers
      ~ND.children
      ~ND.cpu
      ~ND.cuda
      ~ND.double
      ~ND.eval
      ~ND.extra_repr
      ~ND.float
      ~ND.forward
      ~ND.get_buffer
      ~ND.get_parameter
      ~ND.get_submodule
      ~ND.half
      ~ND.load_state_dict
      ~ND.modules
      ~ND.named_buffers
      ~ND.named_children
      ~ND.named_modules
      ~ND.named_parameters
      ~ND.parameters
      ~ND.register_backward_hook
      ~ND.register_buffer
      ~ND.register_forward_hook
      ~ND.register_forward_pre_hook
      ~ND.register_full_backward_hook
      ~ND.register_parameter
      ~ND.requires_grad_
      ~ND.share_memory
      ~ND.state_dict
      ~ND.to
      ~ND.to_empty
      ~ND.train
      ~ND.type
      ~ND.xpu
      ~ND.zero_grad
    
    
